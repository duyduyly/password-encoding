server:
  port: 0002
  servlet:
    context-path: /api/password-encoding
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/<db>
    password: <password>
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
